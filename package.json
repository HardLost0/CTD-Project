{
  "name": "ctd-weather-app",
  "version": "1.0.0",
  "description": "A weather application that fetches real-time weather data using WeatherAPI.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["weather", "api", "express", "node"],
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.0.0",
    "express": "^5.1.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}